// ----------------------------------------------------------------------------
// <auto-generated>
// This is the schema for {{ obj.title }}, the properties of {{ 'wire object type' if wire_obj else 'object' }} `{{ obj.name }}`.
// {{ obj.description }}
//
// CAUTION: This file is autogenerated from https://github.com/arenaxr/arena-schemas. Changes made here may be overwritten.
// </auto-generated>
// ----------------------------------------------------------------------------

using System;
using System.Collections.Generic;
using System.Runtime.Serialization;
using System.ComponentModel;
using Newtonsoft.Json;
using Newtonsoft.Json.Converters;
using Newtonsoft.Json.Linq;
using Newtonsoft.Json.Serialization;
using UnityEngine;
using ArenaUnity.Schemas.Includes;

namespace ArenaUnity.Schemas
{
    /// <summary>
    /// {{ obj.title }}
    /// </summary>
    [Serializable]
    public class Arena{{ cs_class_name }}Json
    {
{% if wire_obj %}
        public readonly string object_type = "{{ obj.name }}";
{% else %}
        [JsonIgnore]
        public readonly string componentName = "{{ obj.name }}";
{% endif %}
        // {{ obj.name }} member-fields
{% for p in properties %}
{% if p.enum and p.cs_type == 'string' %}
        public enum {{ p.enum_name }}
        {
{% for enum in p.enum %}
            [EnumMember(Value = "{{ enum }}")]
            {{ enumcase(enum) }},
{% endfor %}
        }
        private static {{ p.enum_name }} def{{ p.cs_name }} = {% if p.default is not none %}{{ p.enum_name }}.{{ enumcase(p.default) }}{% else %}0{% endif %};
        [JsonConverter(typeof(StringEnumConverter))]
        [JsonProperty(PropertyName = "{{ p.name }}")]
        [{{ "Obsolete" if p.deprecated else "Tooltip" }}("{{ p.description | replace('\n', ' ') | replace('"', '\\"') }}")]
        public {{ p.enum_name }} {{ p.cs_name }} {{ "{ get; protected set; }" if p.deprecated else "" }} = def{{ p.cs_name }};
{% else %}
        private static {{ p.cs_type }} def{{ p.cs_name }} = {% if p.default_formatted %}{{ p.default_formatted }}{% else %}null{% endif %};
        [JsonProperty(PropertyName = "{{ p.name }}")]
        [{{ "Obsolete" if p.deprecated else "Tooltip" }}("{{ p.description | replace('\n', ' ') | replace('"', '\\"') }}")]
        public {{ p.cs_type }} {{ p.cs_name }} {{ "{ get; protected set; }" if p.deprecated else "" }} = def{{ p.cs_name }};
{% endif %}
        public bool ShouldSerialize{{ p.cs_name }}()
        {
{% if p.deprecated %}
            return false; // deprecated in json schema
{% elif p.required %}
            return true; // required in json schema
{% else %}
            // {{ p.name }}
            return ({{ p.cs_name }} != def{{ p.cs_name }});
{% endif %}
        }
{% endfor %}
        // General json object management
        [OnError]
        internal void OnError(StreamingContext context, ErrorContext errorContext)
        {
            Debug.LogWarning($"{errorContext.Error.Message}: {errorContext.OriginalObject}");
            errorContext.Handled = true;
        }

        [JsonExtensionData]
        private IDictionary<string, JToken> _additionalData;
    }
}

/**
 * Open source software under the terms in /LICENSE
 * Copyright (c) 2021-2024, Carnegie Mellon University. All rights reserved.
 */

// CAUTION: This file is autogenerated from https://github.com/arenaxr/arena-schemas. Changes made here may be overwritten.

using System;
using System.Collections.Generic;
using System.Runtime.Serialization;
using System.Text.RegularExpressions;
using Newtonsoft.Json;
using Newtonsoft.Json.Converters;
using Newtonsoft.Json.Linq;
using Newtonsoft.Json.Serialization;
{% if uses_converter %}
using ArenaUnity.Schemas.Converter;
{% endif %}
using UnityEngine;

namespace ArenaUnity.Schemas
{
    /// <summary>
    /// Wraps all attributes in JSON.
    /// </summary>
    [Serializable]
    public class ArenaDataObjectJson
    {
        // data member-fields

        [JsonProperty(PropertyName = "object_type")]
        [Tooltip("3D object type.")]
        public string object_type = null;
        public bool ShouldSerializeobject_type()
        {
            return (object_type != null);
        }
{% for p in inline_properties %}

        private static {{ p.cs_type }} def{{ p.cs_name }} = {% if p.default_formatted %}{{ p.default_formatted }}{% else %}null{% endif %};
{% if p.is_ref %}
        [JsonProperty(PropertyName = "{{ p.name }}", ObjectCreationHandling = ObjectCreationHandling.Replace)]
{% else %}
        [JsonProperty(PropertyName = "{{ p.name }}")]
{% endif %}
{% if p.bool_object_converter %}
        [JsonConverter(typeof({{ p.bool_object_converter }}))]
{% elif p.json_converter %}
        [JsonConverter(typeof({{ p.json_converter }}))]
{% endif %}
        [{{ "Obsolete" if p.deprecated else "Tooltip" }}("{{ p.description | replace('"', '\\"') }}")]
        public {{ p.cs_type }} {{ p.cs_name }}{{ " { get; protected set; }" if p.deprecated else "" }} = def{{ p.cs_name }};
        public bool ShouldSerialize{{ p.cs_name }}()
        {
{% if p.deprecated %}
            return false; // deprecated in json schema
{% elif p.required %}
            return true; // required in json schema
{% else %}
            // {{ p.name }}
            return ({{ p.cs_name }} != def{{ p.cs_name }});
{% endif %}
        }
{% endfor %}
{% for group in property_groups %}

        // {{ group.label }} attributes
{% for p in group.properties %}

        private static {{ p.cs_type }} def{{ p.cs_name }} = {% if p.default_formatted %}{{ p.default_formatted }}{% else %}null{% endif %};
{% if p.is_ref %}
        [JsonProperty(PropertyName = "{{ p.name }}", ObjectCreationHandling = ObjectCreationHandling.Replace)]
{% else %}
        [JsonProperty(PropertyName = "{{ p.name }}")]
{% endif %}
{% if p.bool_object_converter %}
        [JsonConverter(typeof({{ p.bool_object_converter }}))]
{% elif p.json_converter %}
        [JsonConverter(typeof({{ p.json_converter }}))]
{% endif %}
        [{{ "Obsolete" if p.deprecated else "Tooltip" }}("{{ p.description | replace('"', '\\"') }}")]
        public {{ p.cs_type }} {{ p.cs_name }}{{ " { get; protected set; }" if p.deprecated else "" }} = def{{ p.cs_name }};
        public bool ShouldSerialize{{ p.cs_name }}()
        {
{% if p.deprecated %}
            return false; // deprecated in json schema
{% elif p.required %}
            return true; // required in json schema
{% else %}
            // {{ p.name }}
            return ({{ p.cs_name }} != def{{ p.cs_name }});
{% endif %}
        }
{% endfor %}
{% endfor %}

        // General json object management
        [OnError]
        internal void OnError(StreamingContext context, ErrorContext errorContext)
        {
            Debug.LogWarning($"{errorContext.Error.Message}: {errorContext.OriginalObject}");
            errorContext.Handled = true;
        }

        [JsonExtensionData]
        private IDictionary<string, JToken> _additionalData;
    }
}
